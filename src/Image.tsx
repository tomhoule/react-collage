import * as React from "react";
import { Component } from "react";

interface IProps {
    src: any;
}

export class CollageImage extends Component<IProps, {}> {
    render() {
        return (
            <div>
            </div>
        );
    }
}
